/* import { AuthService } from "./auth-service"
import { QueryUserRepository } from "src/query repozitory/queryUserRepository";
import { UsersRepository } from "src/repositories/users-repository";
import { DeviceRepository } from 'src/repositories/device-repository';

describe('integration tests for AuthService', () => {
    
    const deviceRepository = new DeviceRepository(DeviceModel)
    const usersRepository = new UsersRepository(UserModel)
    const queryUserRepository = new QueryUserRepository(UserModel)

    const authService = new AuthService( 
        usersRepository, 
        queryUserRepository,
        deviceRepository,);
    describe('createUser', () => {
        it('should return a user', async() => {
            expect(5).toBe(5)
        })
    })
}) */
